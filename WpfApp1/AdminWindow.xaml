<Window x:Class="WpfApp1.AdminWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp1"
        mc:Ignorable="d"
        Title="Панель администратора" Height="300" Width="400">
    <Grid>
        <StackPanel>
            <StackPanel Orientation="Horizontal" Margin="10">
                <StackPanel>
                    <TextBlock Text="Введите логин:" HorizontalAlignment="Center"/>
                    <TextBox x:Name="NewUserNameBox" Width="150" Margin="5" />
                </StackPanel>
                <StackPanel>
                    <TextBlock Text="Введите пароль" />
                    <PasswordBox x:Name="NewPasswordBox" Width="150" Margin="5" />
                </StackPanel>
                <ComboBox x:Name="RoleComboBox" Width="60" Margin="5" Height="30" VerticalAlignment="Top">
                    <ComboBoxItem Content="User" IsSelected="True" />
                    <ComboBoxItem Content="Admin"  />
                </ComboBox>
                <Button Content="Добавить нового пользователя" Click="AddUserButton_CLick" />
            </StackPanel>
            <ListBox x:Name="UsersList" Height="125" DisplayMemberPath="Username" />
            <Button Content="Разблокировать выбранного пользователя" Click="UnblockButton_Click" Style="{StaticResource ButtonDefaultStyle}" Margin="5" />
            <Button Content="Валидация данных" Click="APIButton_Click" Style="{StaticResource ButtonDefaultStyle}" Margin="5" />
            <TextBlock x:Name="AdminMessageTextBlock" Foreground="Green" Text="" Margin="5" HorizontalAlignment="Center" FontFamily="Times New Roman" FontSize="18" />
        </StackPanel>

        
    </Grid>
</Window>
